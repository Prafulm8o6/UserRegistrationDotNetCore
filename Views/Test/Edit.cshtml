@model UserRegistrationDotNetCore.ViewModels.ManageUserRole

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ManageUserRole</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="hidden" asp-for="@Model.AppUser.Id" />
                <label asp-for="@Model.AppUser.Email">Email : @Model.AppUser.Email</label>
            </div>
            <div class="form-group">

                <h5>Roles</h5>

                @for (int i = 0; i < Model.Roles.Count; i++)
                {
                    <div class="form-check">
                        <input type="hidden" asp-for="@Model.Roles[i].Value" />
                        <input class="form-check-input" asp-for="@Model.Roles[i].Selected" />
                        <label class="form-check-label" asp-for="@Model.Roles[i].Selected">
                            @Model.Roles[i].Text
                        </label>
                    </div>
                }
            </div>
            <div class="form-group">

                <h5>Roles</h5>

                @for (int i = 0; i < Model.Roles.Count; i++)
                {
                    <div class="form-check">
                        <input type="hidden" asp-for="@Model.Roles[i].Value" />
                        <input class="form-check-input" asp-for="@Model.Roles[i].Selected" />
                        <label class="form-check-label" asp-for="@Model.Roles[i].Selected">
                            @Model.Roles[i].Text
                        </label>
                    </div>
                }
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

